# 報告書 構成案

授業の成果報告として提出し、後付資料として使い方説明も載せる。最終的には TeX で組む。
このファイルは**章立てと各章に何を書くか・素材がどこにあるか**を決めるためのもので、
本文そのものではない。

最終更新: 2026-08-15（収録物質の見直し・残バグ修正・図版撮り直しまで反映）

---

## 0. 全体方針

- **二部構成**。第 I 部が成果報告（何を作り、なぜそう作ったか）、第 II 部が使い方説明。
  読者が違う（採点者 / 利用者）ので混ぜない。第 II 部だけ切り離して配れる形にする。
- **「動くもの」を見せるのが主役**。対称操作は静止画では伝わらないので、
  図は「操作前 → 途中 → 操作後」の連続コマで見せる。
- **判断の理由を書く**。何を作ったかより、なぜその設計にしたかが評価対象。
  特に「実装しなかったもの」とその理由は積極的に書く（§I-6）。
- 分量目安: 第 I 部 15〜20 ページ、第 II 部 5〜8 ページ、付録は必要に応じて。

---

## 第 I 部　成果報告

### I-1. はじめに（1〜2 ページ）

- **動機**: 結晶・分子の対称性は、対称要素の記号（点群・空間群）を暗記する形で教えられがちだが、
  「動かして重なる」という本質が体感されにくい。
- **目的**: 対称操作を 3D で実際に動かして見せ、さらにそれを問題として解かせることで、
  記号ではなく操作として対称性を理解させる。
- **成果物**: ブラウザで動く対称性ビューアーと、その上に乗せた 4 種類のクイズ。
- 素材: `docs/PROJECT_SPEC.md` の Purpose、`docs/PUZZLE_CONCEPT.md` §3（教育上の狙い）

### I-2. 対象と背景（2〜3 ページ）

- 対称操作の種類（回転 Cn / 鏡映 σ / 反転 i / 回映 Sn / 回反 -n / らせん / 映進）と、
  分子（点群・Schoenflies）と結晶（空間群・Hermann-Mauguin）で記法が違うこと。
- 既存教材・可視化ツールとの違い。**操作を見せるだけでなく問題として問う**点。
- 図: 操作の種類の一覧図（本アプリのスクリーンショットで作れる）

### I-3. 設計（3〜4 ページ）★ここが評価の中心

**中心的な設計判断: 解析層と表示層を `RenderData` で分離した**

```
CIF/XYZ → 解析(spglib/pymatgen) → RenderData → JSON → 表示(Three.js)
                                        ↓
                                   AtomMapping
                                        ↓
                                  game/ (判定ロジック)
```

書くべき点:

- **表示層は結晶か分子かを知らない**。`RenderData` が差を吸収する（`source_kind` で分岐）。
  結晶は分率座標で解析し `RenderData` で直交座標へ変換、分子は最初から直交座標。
- **判定は必ずサーバー側（Python）**。正解をクライアントに送らない。
  `game/` は `render_data` だけを消費し、DOM にも PyVista にも依存しない純 Python。
  → 各クイズが独立モジュールで、足し引きが容易。
- **1 クイズ = 1 モジュール**（`game/axis_orders.py` など）。試して残す/消すを設計に織り込んだ。
- 素材: `docs/PROJECT_SPEC.md`、`docs/PUZZLE_SPEC.md` §5・§8、`docs/VIEWER_GUIDE.md`

### I-4. 実装（3〜4 ページ）

- 構成と規模（下表）。
- サーバーは Python 標準の `http.server` のみ。外部 Web フレームワークを使っていない。
- 表示は Three.js。API は 25 エンドポイント。

| 項目 | 規模 |
|---|---|
| Python（解析・サーバー） | 約 14,400 行 |
| うち判定ロジック `game/` | 約 1,300 行 |
| JavaScript（ブラウザ表示） | 約 6,100 行 |
| テスト | 約 4,700 行 / 255 件 |
| 収録例題 | 結晶 15 種・分子 21 種（別に32結晶類のテスト用CIF 30 件） |
| API エンドポイント | 25 |

- 図: アーキテクチャ図（上の流れ図を清書）、画面構成図

### I-5. 4 つのクイズ（4〜5 ページ）★分量を割く

各クイズについて「何を問うか / なぜその形式か / 判定の難しさをどう解いたか」を 1 ページずつ。

| クイズ | 問い | 実装 |
|---|---|---|
| 回転軸 | 強調された軸は何回回転軸か | `game/axis_orders.py` |
| 操作あて | アニメで示した操作の種類（＋次数） | `game/operation_identify.py` |
| 合成 | 操作 A→B の合成と同じ 1 操作は何か | `game/composition.py` |
| 移り先 | この原子はどの原子へ移るか（クリック） | `game/atom_mapping.py` |

**書くと面白い技術的な山場**（設計の見せ場になる）:

- **見分けられない操作をどう扱うか**（操作あて）。CO2 の反転と垂直鏡映は動きが同一。
  除外すると CO2 が 0 問になり「対称性が無い」と誤読されるため、**除外ではなく統合**し
  「どちらでも正解」にした。全原子の移動先を丸めた `_visual_signature` で同一視する。
- **恒等と区別できない操作の除外**。平面分子の分子面鏡映は全原子が自分自身に写る＝動かない。
- **出題の偏り**。halite の普通モードは 137 問あるが答えの種類は 7 通り。
  そのまま乱択すると同じ答えばかり出る。**答えを明かさない group ID** を付けて種類均等に抽選。
- **回映/回反の次数は行列の周期でなく回転角から出す**。ベンゼンの主軸は S6 と S3 の両方を担うため、
  軸のラベルを操作名に流用すると「操作 S6」の正解が S3 の像になる。
- **等価な軸の集約**（回転軸クイズ）。正八面体の x/y/z の C4 を 3 回聞くのは冗長なので、
  群操作で軸を写して同値類を作り代表 1 本を出題（PUZZLE_SPEC §3.3b 案 A）。
- 素材: `docs/PUZZLE_SPEC.md`、`docs/sessions/SESSION_REPORT_2026-07-08.md`

### I-6. 作らなかったもの と その理由（1〜2 ページ）★差がつく章

作らなかった判断を根拠付きで書く。スコープ管理ができている証拠になる。

- **○×クイズ（偽の対称要素を混ぜる）**: 構想では「いちばん易しい入口」だったが見送り。
  偽要素の生成則を汎用に作るコストが高い（ずらし量が小さすぎると本物と区別できず、
  大きすぎると自明。妥当な範囲は構造の尺度に依存する）。加えて二択で当てずっぽうが効き、
  他の 4 クイズに比べて体験が薄い。
- **要素探索・閉包パズル**: 保留。群の閉包を体感させる狙いは良いが、UI の設計が別物になる。
- **準結晶そのもの**: 扱えないので構成単位（正 20 面体クラスター）までにした。準結晶は
  並進周期を持たず**単位胞も空間群も無い**ので、格子を前提にした解析経路に載らない。
  無理に載せると spglib が「軸を揃えれば `Pm-3`、傾ければ `P-1`」という嘘を返す
  （エラーは出ない）。一方、有限クラスターとして分子経路に載せれば 5 回軸を正しく
  `Ih`・120 操作として出せる。**扱えないものを扱えるように見せない**という判断。
- **5 回対称をクイズに出すこと**: 回答の選択肢を 2/3/4/6 に閉じたまま維持した。
  5 回軸を持つ構造は 4 クイズすべてから外す。C2/C3 だけ出題すると
  「この物体に 5 回軸は無い」と教えることになるため。
- **スコア・タイムアタック・連続正解**: 入れない。1 問ごとの ○/× のみ。
- **回映軸クイズ**: 見送り（回転軸クイズの拡張だが、操作あてクイズで既に回映を扱う）。
- 素材: `docs/PUZZLE_SPEC.md` §10、`docs/PUZZLE_CONCEPT.md` §3.3・§5

### I-7. 品質保証（2 ページ）

- **255 件のテスト**。層ごとの内訳を表に。
- **headless Chromium による実機テスト 9 件**。人と同じ手順で実際にクイズを遊ぶ。
  「画面に出た操作名がサーバーの公開値と一致するか」「答えるまで 1 秒未満か」など、
  HTTP 境界の検証では捕まえられないものを固定している。
- **回帰の実例を 1 つ載せると説得力が出る**: 合成クイズの結果表示に `（S3）（S6）` という
  自己矛盾した二重表記が出るバグを、修正前のコードに戻すと同じテストが再現させた。
- **「クラッシュしないバグ」の実例**（探索中に発見）: 回映の次数を `round(360/θ)` で
  求めていたため、S10³（θ=108°）が `round(3.33)=3` となり **S3 として出題され、
  正解も S3 と表示されていた**。丸めはどんな角度にも整数を返すので、
  `360/n` が元の角度に戻るかの検算が要る。5 回軸を持つ構造が同梱されていなかったので
  誰も踏んでいなかった＝**テストが無いのではなく、入力が無かった**類の欠陥。
- **バージョン依存の落とし穴**: spglib の対称操作の順序に依存していた箇所があり、
  ライブラリ更新で解析結果が変わった。**一意に決まらない値をテストが固定していた**のが原因。
  特殊位置の原子は席対称群の全操作で生成されるため、生成操作は本来一意でない。
  → 教訓として書ける（「テストは仕様を固定すべきで、実装の偶然を固定してはいけない」）。
- 素材: `docs/sessions/SESSION_REPORT_2026-08-15.md`、`docs/REVIEW_NOTES.md`

### I-8. 既知の限界（0.5〜1 ページ）

隠さず書く。`docs/CLAUDE_HANDOFF.md`「未解決として報告書に載せるもの」に列挙済み。

1. ITC 表記の端ケース。同梱 15 結晶・1168 操作のうち 1163 件（99.6%）は ITC テーブルで
   解決し、計算経路に落ちるのは BaTiO3（R3m）の 5 件だけ。出力は妥当に見えるが
   **原典照合はしていない**
2. 合成クイズはらせん・映進を含む積を扱わない（点操作の積に限定）
3. 収録した結晶構造は理想化した生成物であり、実測 CIF ではない（格子定数と
   Wyckoff 位置は文献値だが、温度因子や部分占有は持たない）

**2026-08-15 に解消した項目**（以前ここに挙げていたもの。§I-7 の材料になる）:

- ブラウザ E2E が 4 クイズ中 2 つ → **4 クイズすべてに追加**（9 件）
- 移り先クイズの guess リングと軸の色が近い → **色相を離した**うえで、
  リング色と軸色の色相差を静的テストで固定（RGB 距離ではなく色相距離。緑と空色は
  チャンネル差が小さくても混同しないため）
- 出題 0 の構造がピッカーに並ぶ → **カタログの実測問題数で絞り、到達不能にした**

### I-9. まとめと今後（0.5 ページ）

- 達成: 解析から判定まで一貫した構成で、4 種類のクイズが動く教材になった。
- 今後: 実際の授業で使ってフィードバックを取る。要素探索・閉包パズルは未着手のまま。

---

## 第 II 部　使い方説明（後付資料）

**単体で配れること**を優先する。第 I 部を読んでいない人が読む前提で書く。

### II-1. 準備（1 ページ）

```bash
git clone https://github.com/ono1105/symmetry_view.git
cd symmetry_view
scripts/setup.sh
scripts/serve.sh --mode puzzle
```

- **注意として明記**: `scripts/setup.sh` は Python の依存に加えて Three.js（`npm ci`）も入れる。
  これを飛ばすと画面は出るのに構造が描画されない。
- 動作環境（Python 3.11+ / Node.js / 対応ブラウザ）。
- PyVista は不要（既定で入らない）ことを一言。

### II-2. 画面の見方（1〜2 ページ）

- 起動 → パズル / 解析の選択画面。
- パズル画面の各部（描画エリア、元素ラベル、投影切替、カメラ操作、問題エリア）。
- 図: 番号を振ったスクリーンショット 1 枚で説明する。

### II-3. 4 つのクイズの遊び方（2〜3 ページ）

各クイズ 0.5〜1 ページ。**「問題の画面 → 答えた後の画面」の 2 枚**を並べる形式で統一。

- 回転軸クイズ / 操作あてクイズ（普通・難しい）/ 合成クイズ / 移り先クイズ
- 操作あての「難しい」ではらせん・映進の並進成分も答えることを明記。
- 移り先クイズは分子のみ。

### II-4. 解析モードの使い方（1〜2 ページ）

- 対称操作の一覧と選択、アニメーション再生、対称要素の表示。
- 自分の CIF / XYZ を読み込む方法。
- PNG / GIF 書き出し。

### II-5. 困ったとき（0.5 ページ）

- 構造が表示されない → `npm ci` を実行したか（最頻出）
- `--with-pyvista` でエラー → `requirements-pyvista.txt` を入れる
- 収録されている例題の一覧（結晶 15 / 分子 21）

---

## 図表リスト（先に用意するもの）

**アーキテクチャ図（#1）以外は取得済み**。`docs/figures/` にあり、対応表は
`docs/figures/README.md`。手で撮ったのではなく `tools/capture_report_figures.py` が
実際の画面を操作して保存するので、UI を直したら撮り直せる。

| # | 図 | 用途 | 状態 |
|---|---|---|---|
| 1 | アーキテクチャ図 | I-3 | **未**（作図。TikZ か描画ツール） |
| 2 | 対称操作の種類一覧 | I-2 | 済（分子・結晶の操作記号の列＋一覧画面 4 枚） |
| 3 | 操作アニメの連続コマ（前・中・後） | I-2, I-5 | 済（ベンゼン C6・3 枚） |
| 4 | パズル画面の各部説明 | II-2 | 済（番号は後付け） |
| 5〜8 | 各クイズの出題画面と回答後画面 | I-5, II-3 | 済（10 枚。操作あては普通・難しいの 2 組） |
| 9 | 対称要素の表示（軸・面・中心・映進矢印） | I-5, II-4 | 済（軸・面・中心・S6・映進・らせんの 6 枚） |
| 10 | テスト実行結果 | I-7 | 済（`fig10_test_output.txt`） |

使った構造: ベンゼン（S3/S6 の話に使えるので主役）、メタン（移り先クイズ）、
halite（NaCl、結晶側）。水・CO2 は結局使っていない。

---

## 残作業

| # | 作業 | 状態 |
|---|---|---|
| 1 | PyVista を任意依存へ分離 | **完了**（`9b56747`） |
| 2 | 通し確認 ＋ 上の図表リストのスクリーンショット取得 | **完了**（アーキテクチャ図を除く） |
| 2b | ブラウザ E2E を回転軸・操作あてにも追加 | **完了**（8 件に） |
| 3 | 旧コード整理 | **完了**（下記） |
| 4 | 収録物質の見直し（教材向けに入れ替え） | **完了**（下記） |
| 5 | 残バグ・UI 改善 | **完了**（色・出題ゼロ構造・内部クリーンアップ） |
| 6 | 図版の撮り直し | **完了**（26 枚。取得中に不具合 2 件が出たので下記） |
| 7 | 準結晶クラスターの追加・残バグ解消 | **完了**（2026-08-16。下記） |
| 8 | 本アウトラインに沿って TeX で執筆 | **次**。アーキテクチャ図（#1）はこの中で作る |

### 準結晶クラスターと残バグ（#7）

- **準結晶は構成単位までにした**（§I-6 に書く）。正 20 面体クラスター 2 件を分子として追加。
  13 原子の WAl12 と 54 原子の Mackay 二十面体（中心空位）。どちらも `Ih`・120 操作
- **解析専用の仕組みを計算で表した**。`beyond_quiz_vocabulary` が真の構造は 4 クイズから外れる。
  手書きリストではなく、クイズ自身の答え生成関数から計算する。問題数 0 の除外では
  足りない（クラスターは 71 問作れてしまう）ことが設計の要点
- **回映次数の検算漏れを修正**（§I-7 の実例）
- 残っていた指摘 4 件を解消: パズル中のポーリング・2 つ目の WebGL の一時停止、
  死んだコード（`read_asymmetric_unit_sites`・`gif_status` の改名）、
  重複の解消（最大移動距離ループ 3 箇所、Python/JS の定数）

### 図版取得で見つかった不具合（#6）

§I-7 の「テストが仕様を固定しているか」の実例になる。詳細は `docs/sessions/SESSION_REPORT_2026-08-15.md`。

- **パズルを遊んだ後、解析モードで例題を開けなかった**。2 つのクライアントがリクエスト ID を
  別々の数列（`Date.now()` と 1 から始まる連番）で発行しており、サーバーの「後勝ち」判定に
  対して連番側が必ず負けていた。エラーは出ず、前の構造が残るだけ。
- **図版取得スクリプトが静かに間違った構造を撮っていた**。読み込み完了を `.operation-row` の
  出現で待っていたが、この行は前の構造のものが残るので何も保証していなかった。
  → 「通し確認」が確認になっていなかった。今は `/api/state` で読み込みを確認する。

### 収録物質の見直しの結果（#4）

報告書に書ける判断なので残す。

- **元の 32 件はテスト都合の選定だった**（32 の結晶類を 1 件ずつ）。128 原子の
  `C12H14N4`、64 原子 P1 の `Babefphite`、`Ho2Rh12As7` など、原子数が多いか
  馴染みがなく、対称操作の学習には向かない。
- **教材向けに入れ替えた**。結晶 15 件（岩塩・CsCl・銅・α鉄・Mg・ダイヤモンド・
  黒鉛・閃亜鉛鉱・ウルツ鉱・蛍石・ルチル・ペロブスカイト・石英・黄鉄鉱・BaTiO3）、
  分子 19 件（既存 11 ＋ C1/C2/Cs/Ci/C2h/C4v/D3d/D3h三方両錐 の 8 件）。
- **文献値から生成した**（`tools/generate_example_structures.py`）。空間群・格子定数・
  Wyckoff 位置と出典が表としてコードに残り、生成後に空間群・原子数・組成を
  読み直して照合する。ダウンロードした CIF を集めるより素性がはっきりする。
- **旧セットは捨てず `tests/fixtures/cif/` へ**。32 結晶類の網羅は
  `tests/test_fixture_cif_coverage.py` が使い、眠っていたファイルが回帰テストになった。
- **選別のハードコードを消した**。パズルの許可リスト（12 件の Set）と分子式テーブル
  （11 件）と「移り先＝分子のみ／難しい＝結晶のみ」の分岐が、すべてカタログの
  `puzzle_counts` から導出されるようになった。§I-3 の「判定はサーバー側」と同じ筋で、
  **クライアントは自分で判断せずデータに従う**という話に使える。

### 旧コード整理の結果（#3）

- `archive/old_gui_attempt/`（64KB・7ファイル）を**削除した**。初回コミット `e625e0f` 以降
  一度も触られておらず、参照しているコードは無い。README が「これができたら用済み」と
  していた6段階（構造解析 → RenderData → 原子対応 → 表示 → アニメ → パズル）は全部終わった。
  必要になったら `git show e625e0f:archive/old_gui_attempt/viewer.py` で戻せる。
- `crystal_viewer/legacy/`（100KB）は**残す**。結晶解析が `symmetry_core.py` の上で動いている。
  `load_legacy_core()` が import ではなくファイルパスで読むため import を grep しても出てこない。
  同じ勘違いが起きないよう `crystal_viewer/legacy/__init__.py` の docstring に書いた。

### 通し確認でわかったこと（#2）

図の材料になるので書き留めておく。詳細は `docs/figures/README.md`。

- **4 クイズすべてが最後まで通る**。回転軸・操作あて（普通／難しい）・合成・移り先を
  実際に遊んで正解まで到達した。未捕捉の JS 例外は 0 件。
- **「どちらでも正解」が実機で出た**。ベンゼンの操作あてで
  `正解（3回回転（C3） または 回映（S3） のいずれも正解）` が表示された。
  §I-5 の「見分けられない操作を除外ではなく統合した」話の実例としてそのまま使える。
- **難しいモードは並進成分まで判定していた**。halite で
  `正解（映進、並進成分 1/4（g(-1/4,1/4,1/2)））`。
- **正解がクライアントに渡っていないことが確認できた**。`/api/puzzle/*` が返す出題データに
  答えは入っておらず、取得スクリプトも当てずっぽうで答えるしかなかった。§I-3 の
  「判定は必ずサーバー側」の裏付けになる。
- **クイズ間で状態が漏れない**。難しいモードの直後に合成クイズへ移っても選択肢は
  点操作の語彙に戻る（`beginCompositionRound()` が明示的に戻している）。

`main` は `origin/main` より 1 コミット進んでいる（図版一式）。push はまだ。
