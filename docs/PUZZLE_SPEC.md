# パズル実装仕様（第1弾）ドラフト

`PUZZLE_CONCEPT.md`（構想）を実装仕様に落とし込むドラフト。ユーザー確認で確定した方針を反映。**レビュー合意用。未実装。**

ステータス: **仕様確定中（実装前）**
最終更新: 2026-07-06

---

## 0. 確定した方針（ユーザー確認済み）

| 論点 | 決定 |
|---|---|
| 対象 | **分子（点群）から**（結晶は後段） |
| モード構成 | 起動時に**「パズル / 解析」選択画面**。入ったら**戻るボタンで選択画面へ戻らないと切替不可**（直接のモード間移動なし＝混乱防止）。CLIフラグで特定モードに直接入れる |
| 最初に作る問題 | **「何回回転軸か」**（軸を1本強調し、成り立つ回転次数を全部選ぶ） |
| 問題タイプの分け方 | ○×・合成 等は**毛色が違うので別モードの問題**として並べる。Lv1/2/3 の段階分けはしない |
| 難易度の2軸 | **問題タイプ（モード）の難易度＝簡単/普通/難しいラベル**（例: 次数当て=簡単、合成=難しい）。**個別の構造の難易度＝☆評価**（水=☆、SF6=☆☆☆） |
| スコア | **なし**（1問ごとに○/×のみ）。将来タイムアタックや連続正解数の追加は保留 |
| 偽の要素 | まず本物だけ（偽要素は後段） |
| 固定色 | **今は作り込まない**。合成クイズ（順序を色で指すと分かりやすい）で必要になったら導入。単純な次数当てでは色は冗長・見づらいので、**実装しながら臨機応変** |

---

## 1. モード構成（アーキテクチャ）

```
起動
  └─ 選択画面（Analysis / Puzzle を選ぶ）
       ├─ Analysis モード … 現在のビューアー（解析・表示・アニメ）
       │     └─[戻る]→ 選択画面
       └─ Puzzle モード … パズルメニュー
             ├─ 問題タイプ一覧（各々に難易度ラベル）
             │    └─ 問題を選んでプレイ
             └─[戻る]→ 選択画面
```

- **選択画面**が既定の入口。モード内には「別モードへ直接切替」する UI は置かず、**[戻る]で選択画面に戻ってから**選び直す。これで「パズル中に急に解析UIに入って混乱/バグ」を防ぐ。
- **モード切替時に状態リセット**（前モードの選択・強調・再生を持ち込まない）。
- **CLI 直接起動**: 例 `--mode analysis` / `--mode puzzle`（未指定なら選択画面）。開発・用途別に直接開ける。

---

## 2. パズルの構成

- パズルは**複数の問題タイプ**を持つ。各タイプに**モード難易度ラベル（簡単/普通/難しい）**を付けて Puzzle メニューに並べる。段階的な「Lv1/2/3」ではなく**独立した問題タイプの集まり**。今回はまず1タイプ（「何回回転軸か」＝簡単）を作る。
- 個別の**構造の難易度は☆**で表す（問題タイプ内で構造を選ぶときに表示）:
  - ☆ やさしい: 水(C2v)・アンモニア(C3v) — 軸が少なく次数 2/3
  - ☆☆ ふつう: ベンゼン(D6h)・XeF4(D4h)・BF3(D3h) — C6/C4 で「2,3,6 を全部」等の気づき
  - ☆☆☆ むずかしい: SF6(Oh) — 軸が多く次数もばらける

**将来の問題タイプ候補**（別モードの問題として順次追加）: 何回回転軸か（今回）／何回回映(Sₙ)か／○×（本物の対称性か・偽要素あり）／要素を全部見つける／操作の合成（色指定が効く）。

---

## 3. 第一実装: 問題「何回回転軸か」

### 3.1 体験（予想 → 答え合わせ）

1. 分子を表示し、**1本の回転軸を強調**（強調は1本だけ。色固定は不要）。
2. 「この軸は何回回すと元に戻る？ **当てはまるものを全部選ぶ**」。選択肢 **2 / 3 / 4 / 6**。
3. **まず予想して選ぶ**。この時点ではアニメで答えを見せない（総当たりで解けてしまうのを防ぐ）。
4. 「答え合わせ」で、各次数を **360/d° 回転アニメ**で確認。**その軸で成り立つ次数の集合と完全一致で正解**（○/×）。

### 3.2 正解の定義（判定ロジック）

- 軸の主次数 n に対し、その軸で成り立つ回転次数 = **n の 2 以上の約数**。
  - C2→{2}／C3→{3}／C4→{2,4}／C6→{2,3,6}。
  - 選択肢 {2,3,4,6} に対する正解部分集合 = `{d ∈ {2,3,4,6} : n mod d == 0}`。
- 判定は**集合の完全一致**（過不足なし）。
- **試し回しの実体** = 360/d° 回転後に全原子が元配置に一致するか（既存アニメ経路＋原子一致/残差判定を流用）。「重なる＝その d は成り立つ」。

### 3.3 端ケース（確定）

- **C∞（線形, CO2 等）**: 主軸を出題し **{2,3,4,6} 全部で正解**（∞ は全ての Cn を含む）。CO2 の垂直 C2 軸（分子軸に垂直）は無限本ゆえ解析が列挙せず、v1 では出題しない。
- **C5・C7 等**（n が 2,3,4,6 と互いに素）: 選択肢内の正解は空集合。→ **v1 出題プールは主次数 2/3/4/6/∞ の軸に限定**（C5/C7 分子は初期除外）。将来「どれも当てはまらない」で対応。

### 3.3b 等価な軸の扱い（要決定）

高対称構造では幾何的に別方向でも**対称等価な軸**が複数ある（正八面体の x/y/z 3本の C4、ベンゼンの6本の C2 など）。「x軸の次数は？→y軸の次数は？」と同じ答えを繰り返すのは冗長。対応案:

- **案A（推奨）: 対称等価クラスで1本に集約**。群操作で軸の向きを写して同値類（軌道）を作り、クラスごとに代表1本を出題。原理的に正しく、他の問題タイプ（「等価な軸は何本？」等）に再利用可能。サーバー側で一度計算。
- **案B（簡易）: 回転次数の集合（＝答え）でグループ化**。同じ答えの軸は1問に集約。実装が軽く x/y 重複も消えるが、答えが同じで対称的に別種の軸（ベンゼンの原子通り C2 と結合通り C2、どちらも {2}）も1つに merge される。

いずれも「区別できる軸からランダムに1本」出題する点は同じ。

### 3.4 出題フロー（確定）

- 構造を選ぶ → その構造の（§3.3b でグループ化した）**区別できる回転軸からランダムに1本**出題。
- 解答して○/×表示 → **「同じ構造でもう一度」/「別の構造にする」**を選べる。
- **スコアなし・ヒントなし**（試し回しで自分で確かめる）。将来タイムアタック等は保留。

---

## 4. 色について（保留・臨機応変）

- 単純な次数当て（この問題）や回映当ては、**1要素だけ強調**すれば足り、固定色は不要。色を足すと逆に見づらい/問題文が冗長になりうる。
- **合成の問題**（順序で結果が変わる）を作るときは、操作を**色で指す**方が分かりやすい可能性が高い。そのタイミングで「要素ごと固定色」を導入検討。
- よって固定色の作り込みは**先送り**し、必要になった問題タイプで実装しながら決める。

---

## 5. アーキテクチャ / API

- 既存の三層（Python 解析＝真実の源 / JSON / three.js 表示）を流用。
- **判定はサーバー側で一貫処理（確定）**。理由: `render_data` の軸は**操作ごとに分割**されており（例: ベンゼンの z 軸は C6用/C3用/C2用/S6用の別要素）、正しい次数集合を出すには **①同一直線の軸要素を幾何グループ化 → ②純回転(rotation_n)だけ抽出（S6等の回映は除外）→ ③次数集合** という非自明処理が要る。2箇所に置くとバグる。マイコンクライアントを薄く保つ方針とも合致。
- 新 API 例: `GET /api/puzzle/axis_orders`（構造の出題可能な軸グループと各正解次数集合を返す）。試し回しは既存 `/api/animation_path`＋原子一致で「その d が成り立つか」を返す/確認。
- モード分割は同一サーバ・同一資産で実現（別ページ・別サーバにはしない）。選択画面＋モード別パネル出し分け＋[戻る]＋`--mode`。

---

## 6. 実装計画（段階）

1. **モード骨組み**: 選択画面（Analysis/Puzzle）＋[戻る]＋状態リセット＋CLI `--mode`。Analysis は現行UIをそのまま収める。
2. **Puzzle メニュー**: 難易度ラベル付きの問題タイプ一覧（今は1タイプ）。
3. **「何回回転軸か」**: 構造ロード→軸1本強調→2/3/4/6 複数選択→試し回し（360/d 回転）→集合一致判定→採点。
4. やさしい分子から順に検証（C2v/C3v→D6h/D4h→Oh、C∞ の扱い確定）。
5. （後段）別の問題タイプ（回映当て／○×＋偽要素／要素探索／合成＋固定色）を順次追加。結晶拡張。

---

## 7. 操作種 → 問題タイプのロードマップ

全操作種を最終的に扱う。操作種で「自然な問いの形」が違うため、問題タイプ（モード）を分けて順次追加する。並進系は完全に後回し。

| 操作種 | 問題タイプ | 問いの形 | 位置づけ |
|---|---|---|---|
| 回転 Cn | 何回回転軸か（**今回**） | 次数を全部選ぶ | 簡単 |
| 回映 Sn（回反） | 何回回映軸か | 次数を全部選ぶ（偶数乗が純回転に化ける注意） | 回転版の拡張 |
| 鏡 σ | 本物の鏡か○×／鏡を全部見つける | 有無・数 | 別モード |
| 反転 i | 反転対称を持つか○× | 有無 | 別モード |
| 全種横断 | 今のアニメは何の操作か判別 | 種類当て | 導入向き |
| 合成 | 見つけた操作の組合せで作れるか | 合成（色指定が効く） | 難しい |
| 並進・らせん・映進 | （後回し） | — | 結晶・最後 |

方針: 1〜4 は**とりあえず作り、実際に遊んでから残すか判断**（`game/` のモジュール単位で足し引き）。

## 8. アーキテクチャ方針

- 判定・出題ロジックは **`crystal_viewer/game/`（純Python・`render_data` のみ消費・PyVista/DOM/生パーサ非依存）** に置く（`VIEWER_GUIDE.md` の Future Game Architecture 準拠）。**判定は一箇所（サーバー）**。
- **各問題タイプ＝ `game/` 内の独立モジュール**（試して残す/消すが容易）。
- three.js/UI は**既存の強調・アニメを再利用**し、当分は新規アニメを増やさない（演出で必要になれば追加可）。
- 判定の**妥当性テスト**を付ける（ベンゼン主軸{2,3,6}＋垂直{2}、SF6 {2,4}/{3}/{2} など一意な教科書値で回帰）。

## 9. 将来の味付け（保留）

ミスカウント／クリア演出／罠（偽要素）／タイムアタック／連続正解数。旧pygame案由来のアイデア。今は入れず、遊んでみてから検討。難易度☆は**とりあえず適当**に付け、不要なら表示を消す。

## 10. 決定事項（確定）

C∞含める・スコアなし・ヒントなし・判定サーバー側・等価軸は**案A（対称等価クラス集約）**・難易度2軸(モード=簡単/普通/難しい, 構造=☆)・出題ランダム1軸・同/別構造の選択・CLI `--mode`・予想→答え合わせフロー。**未決定なし**。
